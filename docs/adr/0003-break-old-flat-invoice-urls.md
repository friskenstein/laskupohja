# Break old flat invoice URLs

The rebuild will not preserve compatibility with existing flat query parameter invoice URLs. We will support only the new versioned shareable invoice URL payload, accepting broken old bookmarks in exchange for a simpler model, cleaner parser, and less migration code in a small app.
