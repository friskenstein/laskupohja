## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `friskenstein/laskupohja`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the repo's `status:*` and `type:*` GitHub labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: read root `CONTEXT.md` and `docs/adr/` when present. See `docs/agents/domain.md`.
